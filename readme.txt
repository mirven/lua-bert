This is the start of a lua implementation of bert. http://bert-rpc.org/

The encoding is nearly complete but lacks support for Time, Regular Expressions, and big numbers.

Lua treats integers as floats so this isn't completely solved yet, should not be a problem for decoding.